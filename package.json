{
  "name": "solidjs-cms",
  "private": true,
  "workspaces": [
    "frontend/*"
  ],
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
